# Enforce strict audit level during installs
audit-level=high

# Save exact versions to reduce unexpected breakage
save-exact=true

# Prevent accidental publish of this private project
package-lock=true
